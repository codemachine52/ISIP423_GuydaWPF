<Page x:Class="WpfApp1.Pages.RegistrationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="RegistrationPage">

    <Grid>
        <Grid.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="#FFCC4444"/>
                <GradientStop Color="#FF153F75" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Button x:Name="Back" Width="100" Height="35" FontSize="20" Background="#FF2D2D2D" FontWeight="Thin" Foreground="White" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="30,0,0,30" Click="Back_Click">Назад</Button>

        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="250">
            <TextBlock Padding="0,0,0,20" FontSize="36" FontWeight="Thin" Foreground="White" HorizontalAlignment="Center">Регистрация</TextBlock>

            <TextBlock FontSize="16" FontWeight="Thin" Foreground="White" Margin="0,0,0,5">Введите логин</TextBlock>
            <TextBox x:Name="LoginText" Height="30" Background="#B2FFFFFF" LostFocus="LoginText_LostFocus"/>
            <TextBlock x:Name="LoginError" Foreground="#FFFF6B6B" FontSize="12" Margin="0,2,0,10" Visibility="Collapsed" TextWrapping="Wrap"/>

            <TextBlock FontSize="16" FontWeight="Thin" Foreground="White" Margin="0,0,0,5">Введите пароль</TextBlock>
            <PasswordBox x:Name="PassText" Height="30" Background="#B2FFFFFF" LostFocus="PassText_LostFocus"/>
            <TextBlock x:Name="PassError" Foreground="#FFFF6B6B" FontSize="12" Margin="0,2,0,10" Visibility="Collapsed" TextWrapping="Wrap"/>

            <TextBlock FontSize="16" FontWeight="Thin" Foreground="White" Margin="0,0,0,5">Подтвердите пароль</TextBlock>
            <PasswordBox x:Name="PassVerificText" Height="30" Background="#B2FFFFFF" LostFocus="PassVerificText_LostFocus"/>
            <TextBlock x:Name="PassVerificError" Foreground="#FFFF6B6B" FontSize="12" Margin="0,2,0,10" Visibility="Collapsed" TextWrapping="Wrap"/>

            <TextBlock FontSize="16" FontWeight="Thin" Foreground="White" Margin="0,0,0,5">Введите ФИО</TextBlock>
            <TextBox x:Name="FIOText" Height="30" Background="#B2FFFFFF" LostFocus="FIOText_LostFocus"/>
            <TextBlock x:Name="FIOError" Foreground="#FFFF6B6B" FontSize="12" Margin="0,2,0,10" Visibility="Collapsed" TextWrapping="Wrap"/>

            <TextBlock FontSize="16" FontWeight="Thin" Foreground="White" Margin="0,0,0,5">Электронная почта</TextBlock>
            <TextBox x:Name="EmailText" Height="30" Background="#B2FFFFFF" LostFocus="PhoneText_LostFocus"/>
            <TextBlock x:Name="EmailError" Foreground="#FFFF6B6B" FontSize="12" Margin="0,2,0,20" Visibility="Collapsed" TextWrapping="Wrap"/>

            <Button x:Name="RegistrUser" Height="40" FontSize="20" Background="White" FontWeight="Bold" Foreground="Black" Click="RegistrUser_Click">Зарегистрироваться</Button>
        </StackPanel>
    </Grid>
</Page>
