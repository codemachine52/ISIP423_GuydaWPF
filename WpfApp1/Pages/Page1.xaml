<Page x:Class="WpfApp1.Pages.Page1"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Page1">

    <Grid Background="Black">
        <Border Background="RosyBrown"  Height="45" VerticalAlignment="Top">
            <Grid>
                <TextBlock Text="Кинотеатр Мечта" VerticalAlignment="Center" FontSize="20" HorizontalAlignment="Left" Margin="60,0,0,0" Foreground="White" FontWeight="Bold"></TextBlock>
                <Button Width="50" HorizontalAlignment="Right" Background="{x:Null}" Content="👤" FontSize="20" BorderBrush="{x:Null}" Margin="0,0,60,0"/>
            </Grid>
        </Border>
        <Border CornerRadius="10" Height="25" Background="Azure" VerticalAlignment="Top" Margin="60,10,10,10" Width="250" HorizontalAlignment="Center">
            <TextBox x:Name="FindFilm" Height="20" Background="{x:Null}" BorderBrush="{x:Null}" SelectionBrush="{x:Null}" Foreground="Black" Margin="10,0,10,0" FontSize="14" />
        </Border>
        <ListBox x:Name="listBox" 
                 Margin="25,70,25,70"
                 Background="Transparent"
                 BorderThickness="0"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled" d:ItemsSource="{d:SampleData ItemCount=5}" SelectionChanged="listBox_SelectionChanged">

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <!-- Карточка товара -->
                    <Border BorderBrush="#E0E0E0" 
                            BorderThickness="1"
                            CornerRadius="6"
                            Background="White"
                            Margin="12,8"
                            Width="240"
                            Height="290">

                        <Border.Effect>
                            <DropShadowEffect BlurRadius="6" ShadowDepth="1" Color="#CCCCCC"/>
                        </Border.Effect>

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="140"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Область картинки -->
                            <Border Grid.Row="0" 
                                    Background="#F8F8F8" 
                                    CornerRadius="6,6,0,0">
                                <Image Height="140" VerticalAlignment="Center" UseLayoutRounding="False" Source="{Binding ImagePath}" Width="240" />
                            </Border>

                            <!-- Информация -->
                            <StackPanel Grid.Row="1" 
                                        Margin="15,10">
                                <TextBlock Text="{Binding Name}" 
                                           HorizontalAlignment="Center" 
                                           TextWrapping="Wrap"
                                           FontSize="15" 
                                           FontWeight="SemiBold"
                                           Foreground="#333333"
                                           TextAlignment="Center"/>

                                <!-- Цена в рублях -->
                                <TextBlock Text="{Binding Price, StringFormat='{}{0} ₽'}" 
                                           HorizontalAlignment="Center"
                                           FontSize="16" 
                                           FontWeight="Bold"
                                           Foreground="#E74C3C"
                                           Margin="0,10,0,0"/>
                            </StackPanel>

                            <!-- Кнопка в корзину -->
                            <Button Grid.Row="2"
                                    Content="Добавить в корзину" 
                                    Margin="20,0,20,15"
                                    Padding="10,6"
                                    Click="ToBasket_Click"
                                    Tag="{Binding ID}"
                                    Background="#3498DB"
                                    Foreground="White"
                                    FontSize="14"
                                    FontWeight="SemiBold"
                                    BorderThickness="0"
                                    Cursor="Hand">
                            </Button>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>

            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
    </Grid>
</Page>
