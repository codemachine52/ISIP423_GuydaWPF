<Page x:Class="Anastasia423WPF.Pages.DetailsPC"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Anastasia423WPF.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="DetailsPC">

    <Grid>
        <Grid.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="#FF565656" Offset="1"/>
                <GradientStop Color="#FFEC9987" Offset="0"/>
            </LinearGradientBrush>
        </Grid.Background>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <!--заголовок магазина-->
        <TextBlock Name="title" Grid.Column="0" Grid.ColumnSpan="2"  HorizontalAlignment="Center"
           Margin="0,30,0,0" TextWrapping="Wrap" Text="СОБЕРИ САМ!"
           VerticalAlignment="Top" FontSize="22"
           FontFamily="Batang" FontWeight="Bold"/>
        <StackPanel Grid.Row="1">
            <!--уже выбранные комплектующие-->
        </StackPanel>
        <StackPanel Grid.Row="2" Grid.Column="1">
            <ListBox>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock x:Name="DetailName" Text="{Binding name}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </Grid>
</Page>
